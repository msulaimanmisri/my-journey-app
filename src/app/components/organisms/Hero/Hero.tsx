import { HeroContent } from '@/app/components/molecules/HeroContent/HeroContent';

export function Hero() {
  return (
    <section aria-labelledby="hero-heading">
      <HeroContent />
    </section>
  );
}